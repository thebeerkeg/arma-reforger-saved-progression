# Used by docker-compose when running with --profile postgres.
# Copy to `.env` and change POSTGRES_PASSWORD before bringing the stack up.
# The same password must appear in config.toml's postgres_url.

POSTGRES_USER=tbk
POSTGRES_PASSWORD=changeme
POSTGRES_DB=tbk_progression
